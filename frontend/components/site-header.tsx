"use client";
import Link from "next/link";
import { Activity, LogOut } from "lucide-react";
import { useAuth } from "@/components/auth-provider";
const links = [{ href: "/simulate", label: "Live demo" }, { href: "/plan", label: "Plan a trip" }];
export function SiteHeader() { const { isReady, logout, user } = useAuth(); return <header className="border-b border-slate-800/80 bg-slate-950/80 px-5 py-4 backdrop-blur sm:px-8"><div className="mx-auto flex max-w-6xl items-center justify-between gap-4"><Link href="/" className="flex items-center gap-2 font-bold tracking-tight"><span className="grid h-8 w-8 place-items-center rounded-lg bg-emerald-400 text-slate-950"><Activity size={18} /></span>BoardWise</Link><nav className="flex items-center gap-4 text-sm text-slate-300 sm:gap-6">{links.map((link) => <Link className="hidden transition hover:text-emerald-300 sm:block" key={link.href} href={link.href}>{link.label}</Link>)}{isReady && (user ? <div className="flex items-center gap-3">{user.role === "admin" && <Link href="/admin" className="hidden transition hover:text-emerald-300 sm:block">Admin</Link>}<span className="hidden max-w-36 truncate text-xs text-slate-400 md:block">{user.email}</span><button onClick={logout} className="inline-flex items-center gap-1 text-slate-300 transition hover:text-rose-300"><LogOut size={16} /><span className="hidden sm:block">Sign out</span></button></div> : <Link href="/login" className="button button-primary px-3! py-2!">Sign in</Link>)}</nav></div></header>; }
